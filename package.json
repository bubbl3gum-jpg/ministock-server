{
  "name": "ministock-client",
  "version": "1.0.0",
  "description": "Web server and web client for MiniStock",
  "scripts": {
    "start:backend": "node backend/index.js",
    "start": "npm --prefix backend start",
    "start:dev": "npm --prefix backend run start:dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.3"
  }
}
